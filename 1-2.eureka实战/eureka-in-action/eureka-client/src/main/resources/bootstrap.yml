spring:
  application:
    # 对应 config-serve 中的 eureka-client.yml 文件
    name: eureka-client

  cloud:
    config:
      uri: http://localhost:8888

#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'